# 📱 Како тестирати PWA на телефону (localhost)

## 🚀 Брза метода - 3 минута

### **Корак 1: Пронађи IP адресу рачунара**

Покрени у командној линији:

```powershell
ipconfig
```

Тражи **IPv4 Address** (нешто као `192.168.1.XXX` или `10.0.0.XXX`)

Пример:
```
Wireless LAN adapter Wi-Fi:
   IPv4 Address. . . . . . . . . . . : 192.168.1.105
```

**Твоја IP адреса:** Запамти овај број!

---

### **Корак 2: Покрени Python HTTP Server**

У твом фолдеру (`Restoran Zlatar Novi`), покрени:

```powershell
python -m http.server 8000
```

Видећеш:
```
Serving HTTP on :: port 8000 (http://[::]:8000/) ...
```

**Остави овај terminal отворен!**

---

### **Корак 3: Приступи са телефона**

На телефону, отвори Chrome (Android) или Safari (iOS) и унеси:

```
http://192.168.1.105:8000/index.html
```

(Замени `192.168.1.105` са **твојом IP адресом** из корака 1)

---

## ✅ Треба да видиш:

1. **Твој сајт се учитава са рачунара**
2. **После 5-10 секунди** → Golden button доле лево: "Инсталирај апликацију"
3. **Кликни на button** → Install prompt
4. **Икона на Home Screen-у!** 🎉

---

## 🔥 АЛТЕРНАТИВА - ngrok (за HTTPS)

PWA треба **HTTPS** за неке функције. Ако хоћеш full тест:

### **1. Преузми ngrok:**
https://ngrok.com/download (бесплатно)

### **2. Покрени Python server (port 8000)**

### **3. Покрени ngrok:**
```powershell
ngrok http 8000
```

### **4. Копирај HTTPS линк:**
```
Forwarding  https://abc123.ngrok.io -> http://localhost:8000
```

### **5. Отвори на телефону:**
```
https://abc123.ngrok.io/index.html
```

**Предности ngrok:**
- ✅ Прави HTTPS (сви PWA features раде)
- ✅ Push notifications ће радити
- ✅ Можеш поделити са другима

---

## ⚠️ Troubleshooting

**Телефон не може да приступи?**
1. Провери да ли су рачунар и телефон на **истој WiFi мрежи**
2. Proveri да ли firewall блокира порт 8000
3. Windows Firewall → Allow Python

**Install button се не појављује?**
- Користи Chrome на Android (Safari на iOS има другачији UI)
- Refresh страницу (Ctrl+Shift+R)
- Провери Console (F12) за грешке

**Икона не изглед добро?**
- Тренутно користим постојећу слику
- Ако хоћеш специјалну PWA икону, реци ми

---

## 🎯 Брзи тест - Copy/Paste:

```powershell
# 1. Нађи IP
ipconfig

# 2. Покрени server у фолдеру сајта
cd "C:\Users\WEB STUDIO LINK\OneDrive\Desktop\Restoran Zlatar Novi"
python -m http.server 8000

# 3. На телефону отвори:
# http://TVOJA-IP:8000/index.html
# (замени TVOJA-IP са бројем из ipconfig-а)
```

---

**Јави ми када покренеш - помоћи ћу ако заглави!** 🚀
